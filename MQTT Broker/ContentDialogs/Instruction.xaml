<UserControl
    x:Class="MQTT_Broker.ContentDialogs.Instruction"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MQTT_Broker.ContentDialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <!--<ContentDialog
    
        Title="Light Instruction"
        PrimaryButtonText="Got It">

        <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Image x:Name="ArduinoSketch"
                VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
            <TextBlock x:Name="Instruction" Margin="5"/>
            <TextBlock x:Name="Components" Margin="5"
                       Foreground="Orange">
                <Run Text="Components: "/>
                <Run Text=""/>
            </TextBlock>
            <TextBlock Foreground="Green" Margin="5">
                <Run Text="Pin: "/>
                <Run Text=""/>
            </TextBlock>
        </StackPanel>

    </ContentDialog>-->
</UserControl>
